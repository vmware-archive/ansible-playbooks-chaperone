---
- name: prepare variables files #[XXX] hardcoded in playbook
  hosts: local
  tasks:
  - name: prepare vcenter variables 
    template: src="/opt/supervio-ansible/roles/{{ item }}/defaults/main.yml.j2" dest="/opt/supervio-ansible/roles/{{ item }}/defaults/main.yml.j2"
    with_items:
    - vcenter
    - supervio
    - autodeploy
  vars_files:
    - /var/lib/supervio/answerfile.yml
